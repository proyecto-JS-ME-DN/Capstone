<!DOCTYPE html>
<html lang="es-es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacto</title>
    <%- include('partials/bootstrapcss') %>
    <%- include('partials/estilos') %>
</head>
<body class="sub_page">
  <div class="hero_area">
    <%- include('partials/navigation_bar', { currentPage: "contacto", activeRole: role }) %>
  </div>
  <%- include('partials/contactosection') %>
  <div class="col-lg-7 col-md-6 px-0">
    <div class="map_container">
      <div class="map">
        <div id="googleMap"></div>
      </div>
    </div>
  </div>
  <%- include('partials/sweetalert') %>
</body>
<%- include('partials/sub_footer') %>
<%- include('partials/footer') %>
<%- include('partials/scripts') %>
<script>
  function initMap() {
    var location = {lat: -33.673127, lng: -70.987603}; // Coordenadas
    var map = new google.maps.Map(document.getElementById('googleMap'), {
      zoom: 15,
      center: location
    });
    var marker = new google.maps.Marker({
      position: location,
      map: map
    });
  }
  
  // Carga la API de Google Maps de forma asíncrona
  function loadScript() {
    var script = document.createElement('script');
    script.src = "https://maps.googleapis.com/maps/api/js?key=AIzaSyCqj1rBqsaLuZwEgTvL6-VYK2W06eZiNgI&callback=initMap";
    script.async = true;
    document.body.appendChild(script);
  }
  
  window.onload = loadScript;
</script>
</html>